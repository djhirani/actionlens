# Required server-only secret. Never prefix with NEXT_PUBLIC_ or commit a value.
OPENAI_API_KEY=

# Required model ID. The measured Build Week run used gpt-5.6.
OPENAI_MODEL=gpt-5.6

# Optional. Photo upload is enabled unless this is explicitly false.
PHOTO_INPUT_ENABLED=true
